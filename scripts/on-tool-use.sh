#!/usr/bin/env bash
# Feed tool use events to the buddy engine via MCP.
# This script is called by the PostToolUse hook.
# The hook passes tool info via environment variables.
# For now, this is a passthrough — the MCP server handles state.
exit 0
