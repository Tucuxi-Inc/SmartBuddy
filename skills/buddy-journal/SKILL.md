---
name: buddy-journal
description: View your buddy's journal entries for the current project
---

# Buddy Journal

Read the journal file at `${CLAUDE_PLUGIN_DATA}/projects/{project_hash}/journal.md` if it exists. Display the entries chronologically. If no journal exists yet, explain that the buddy writes journal entries when the cognitive engine selects the "journal" action during sessions.
